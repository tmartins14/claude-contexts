# Football Analytics — Progress Log

Append-only. Newest at top. One line per meaningful ship or decision — not per commit.
Format: `YYYY-MM-DD — <what shipped / what was decided>`

## Log
- 2026-08-15 — Fixed goalMouthShotPanel.js and cumulativeXtChart.js hardcoding
  light-theme hex literals for their background rect and several supporting
  strokes/text (legend, ground line, off-target strip, axis ticks, tooltip) —
  reported live on tylermartins.com's player-match-analysis page as "still has
  a white background" in dark mode. Fixed by switching to `var(--token,
  #fallback)`, matching the pattern already used correctly in
  cumulativeXgChart.js. Bumped footballd3 to **0.1.4 — not yet published to
  npm**; tylermartins.com's Vercel builds install from the registry, not the
  local workspace symlink, so this fix won't reach production until it's
  published and the site's dependency is bumped. Committed to
  `player-match-analysis` branch (`aebc8f7`), pushed.

- 2026-08-09 — Added a 1×/2×/4× pace multiplier to the Timeline card
  (Highlights and All-events modes both benefit, All-events especially).
  Found and fixed a real bug while wiring it up: highlightReel.js's
  `update({stepDurationMs})` only reassigned the variable a running
  `setInterval` had already captured by value, so changing speed mid-play
  silently did nothing until the *next* fresh Play press — a plain JS
  interval can't retroactively change its own delay. Fixed by extracting
  the tick logic into a shared `startTimer()` that both `play()` and a
  live speed change now call, clearing and rescheduling the timer without
  resetting the current moment or playing state. tsc/eslint/8-8 e2e/148
  vitest all green; committed locally on both repos' player-match-analysis
  branches, still unpushed. This was flagged as likely the last change for
  this feature.
- 2026-08-09 — Team-colored every chart in the Player Match Analysis popup
  (cumulative xT, territory pitch markers/heatmap/hull, pass sonar,
  goal-mouth, action feed glyphs, popup header avatar/badge) — each now
  renders in the selected player's true team color (Spain red / England
  blue) instead of the generic focal/secondary tokens; buttons keep focal.
  Moved the cross-link hover-highlight color from secondary to focal, since
  secondary is identical to England's true color in light mode and would've
  been invisible against an England player's chart. Also fixed a real gap:
  `/football/player-match-analysis` had been fully built all session but
  was reachable only by typing the URL — added it to the home page and
  `/football` section index, matching the existing dashboard page's nav
  pattern exactly, plus a breadcrumb label. tsc/eslint/8-8 e2e/145 vitest
  all green; committed locally on both repos' player-match-analysis
  branches, still unpushed.
- 2026-08-09 — Fifth browser-review round: fixed a real state-leak bug where
  the new combined Timeline card's Play kept running after switching
  players (its D3 reel controller's setInterval was never stopped on
  unmount, so the old player's moments silently overwrote the new player's
  scrub position on a timer) and a real SSR/client hydration mismatch in
  PlayerMatchAnalysisClient.tsx (confirmed via the browser console's own
  attribute diff — resolvedTheme read directly in JSX instead of gated
  behind a mounted flag). Also fixed three visual issues: the Highlights
  toggle's missing right border when active (needed the same z-index
  layering the team toggle already had), the Territory panel's pitch never
  got the earlier pitch-color swap, and the player-hero selection ring is
  now team-colored instead of always focal red (reversed an earlier
  decision after seeing it look wrong on England's blue nodes) — plus
  scoped the popup's scroll-into-view to only fire on the initial open, not
  every subsequent player switch. tsc/eslint/8-8 e2e all green; committed
  locally on tylermartins.com's player-match-analysis branch, still
  unpushed.
- 2026-08-09 — Fourth browser-review round of Player Match Analysis: merged the Highlight Reel and Master Timeline into one full-width Timeline card with a Highlights/All-events playback toggle (one shared Play control instead of two), made Match Contribution full-width beneath it, swapped the player-hero header and lineup pitch to opposite beige tones, and replaced event-type color-coding with shape+outcome coding (shape = category, filled/hollow = success/failure) shared identically between the Territory pitch and Action Feed. Fixed two real bugs along the way: the Action Feed lost scroll position on every hover (a full remount was keyed to hover state, not just structural props) and had inconsistent row indentation (an unpinned flex-basis let a long outcome string shrink the minute column on some rows but not others — confirmed via live DOM measurement, not just code reading). All gates green (115 pytest, 143 vitest, docs gate, both repos' eslint/tsc, 8/8 e2e); both repos committed locally on player-match-analysis, still unpushed.
- 2026-08-08 — Redid the prior round's card-whitespace fix and caught a real tooltip bug. The earlier `items-start` fix (aligning cards to their own content height) just relocated the whitespace to uneven card bottoms instead of using it — corrected per explicit user direction to stretch cards back to match their siblings and redistribute/scale the shorter card's own content instead: Highlight reel now centers its (slightly enlarged) content within a card height that exactly matches Match contribution's (measured: both 263px), and Territory & events distributes its chip row + pitch evenly across a card that now bottom-aligns exactly with Action feed (measured: both end at the same pixel). Also fixed a real bug while investigating: hovering any event on the Territory pitch showed "undefined" (a tooltip built for a different, multi-player possession-scoped consumer unconditionally rendered a player-name line) plus a bare, context-free "+59.0s" (the raw StatsBomb seconds-within-minute field, never a meaningful timestamp on its own) — tooltip now skips the player line when absent and prefers an actual match minute when given, confirmed the showcase gallery's own (different) use of the same component is unaffected. Saved a memory note on the corrected card-whitespace approach for future layout work. All gates green (115 pytest, 124 vitest, docs gate, both repos' eslint/tsc, 8/8 e2e); both repos committed locally on player-match-analysis, still unpushed.
- 2026-08-08 — Three small Player Match Analysis fixes: doubled the master scrubber's full-event autoplay speed (900ms/step, was 1800ms — it steps through every event, not just 3-5 highlights, so the slower cadence felt sluggish); added the actual formatted xT/xG value next to the action feed's swing bar (previously bar-only, had to eyeball it); and fixed dead whitespace in the Highlight Reel and Territory & Events cards — root cause was CSS Grid's default stretch alignment forcing a short card to match a much taller sibling's height, fixed with `items-start` on the two affected grid rows. All gates green (115 pytest, 121 vitest, docs gate, both repos' eslint/tsc, 8/8 e2e); both repos committed locally on player-match-analysis, still unpushed.
- 2026-08-08 — Rebuilt the Player Match Analysis lineup selector card against a detailed Claude Design spec. Resolved a real conflict first: the spec's prose described a dual-team pitch, but its own cited reference implementation only shows one team at a time (matching the existing R1 design) — confirmed with the user to keep one-team-at-a-time. Moved the match-header block (kicker/scoreline/match details) from the page into the sticky left-panel card to match the reference's actual structure; added true Spain (#C60B1E)/England (#1E3A5F) color tokens distinct from the app's generic focal/secondary reuse, scoped to this card only (the popup's own avatar badge is untouched); replaced the generic team toggle with a bespoke swatch+color one showing each team's real recorded formation instead of a hardcoded placeholder; fixed the selection ring to always be focal regardless of team (was inconsistently alternating) and gave the selected bench row a filled background + hover state instead of just an outline. Along the way hit and fixed a genuine React/JSX quirk: a `{expression}` immediately followed by a literal string starting with a space renders with that space silently swallowed into a hydration-boundary comment node. All gates green (115 pytest, 117 vitest, docs gate, both repos' eslint/tsc, 8/8 e2e); both repos committed locally on player-match-analysis, still unpushed.
- 2026-08-08 — Fixed 5 more issues from a third browser-review round of Player Match Analysis, including a real bug: the new master-scrubber autoplay (from the prior round) died after one step because its D3-remount effect's cleanup also stopped autoplay, and that cleanup fired on any incidental width change from sibling panels resizing — not just deliberate stops. Split autoplay's lifecycle from the DOM-remount effect to fix it. Also matched its Play button styling to the Highlight Reel's, added titles to the four cards that had none, moved the territory panel's event-type filter chips above the pitch, and scaled goal-mouth shot markers to the frame's now-dynamic height (a fixed 22px radius was up to ~47% of a narrow frame's height after the prior round's aspect-ratio fix). All gates green (115 pytest, 114 vitest, docs gate, both repos' eslint/tsc, 8/8 e2e); both repos committed locally on player-match-analysis, still unpushed.
- 2026-08-07 — Fixed 3 more issues from a second browser-review round of Player Match Analysis: added a new autoplay control to the Master Scrubber (steps through every one of the player's own event minutes, separate from the Highlight Reel's own curated-moments Play) that stops cleanly on a real drag/click; fixed the goal-mouth panel's aspect ratio, which was pinned to a fixed config.height independent of the measured container width and got squashed toward square whenever the container was narrower than expected — height now derives from width at the real ~3:1 goal ratio; and fully rebuilt the Highlight Reel against a detailed design spec + its reference implementation (a `Player Match Analysis.dc.html` design-handoff file) — new moment selection (goals + single best non-goal shot + top-3 positive-xT actions, capped at 5, matching the reference's real quirk that this is a plain chronological truncation with no goal-priority protection), a simplified onScrubTo/onHoverEvent API, and a full visual + light/dark theme rebuild. All gates green (115 pytest, 113 vitest, docs gate, both repos' eslint/tsc, 8/8 e2e); both repos committed locally on player-match-analysis, still unpushed.
- 2026-08-07 — Fixed 5 issues found in first browser review of Player Match Analysis (plus one more caught along the way): eventScatter.js crashed Next.js SSR (module-top-level document.createElement, now lazy-init like every other component); heatmap.js's smooth render path assumed canvas-x mapped directly to grid columns, which only held for horizontal-orientation pitches — TerritoryPanel's vertical pitch showed a transposed density surface, fixed by inverting through the pitch's own d3 scales; goalMouthShotPanel.js redesigned to actually look like a net (crosshatch mesh pattern, thicker posts, receding side wedges, ground line) instead of a plain box; highlightReel.js's bare play/step buttons and dots got real styling matching the app's button language; PlayerMatchAnalysisClient.tsx's two-column popup split and the mobile full-overlay rule both had breakpoints that overlapped in 900-1023px, rendering broken/doubled layout in that range — closed the gap and tightened padding/fixed heights throughout while at it. The "cumulative xT decreases for some players" report turned out to be correct behavior (real negative-xT passes), not a bug. All gates green (115 pytest, 109 vitest, docs gate, both repos' eslint/tsc, 8/8 e2e); both repos committed locally on player-match-analysis, still unpushed pending review.
- 2026-08-07 — Shipped the tylermartins.com side of Player Match Analysis: new /football/player-match-analysis route (PlayerMatchAnalysisClient + 9 new panel components + 2 API routes for on-selection player_events/heatmap_buckets fetches), e2e/player-match-analysis.spec.ts (8 tests, incl. mobile 390px overlay). Manual browser verification against the real page caught two more real bugs beyond the R1/R2 revision: goalMouthShotPanel.js read shot_end_location as [y,z] instead of StatsBomb's actual [x,y,z] (every shot plotted off-frame), and formation.js's bench defaulted to 2 columns which overlapped text at the narrow single-team width this page uses — both fixed with regression tests. Both repos' work is committed locally on branch player-match-analysis (football-analytics) / player-match-analysis (tylermartins.com), neither pushed yet — pending review.
- 2026-08-06 — Revised the football-analytics side of Player Match Analysis after review: replaced the dual-pitch lineupSelector with an extended formation.js (selectedId ring + optional bench) since the page shows one team at a time with a toggle, not two teams on one pitch; made all six playerStatCards metrics scrub-reactive (added assisted_shot_xg/possession_shot_xg to extract_player_events.py, new extract_possession_shares.py) instead of three being fixed match-totals, removing extract_player_match_summary.py. 115 pytest + 94 vitest green. Second commit on player-match-analysis, still unpushed.
- 2026-08-06 — Finished the football-analytics side of Player Match Analysis on branch player-match-analysis: extended extract_player_events.py (shot_xg, shot_end_location, is_goal, key_pass), added extract_player_heatmap_buckets.py (minute-bucketed KDE grids, since heatmap.js's existing extractor is static full-match only) and extract_player_match_summary.py (xg/xa/xg_chain/padj_defensive_actions), and built the remaining footballd3 components the handoff called for: passSonar, actionFeed, goalMouthShotPanel, playerStatCards, highlightReel, lineupSelector, plus an inbound highlightEventId cross-link on cumulativeXtChart.js. 114 pytest + 89 vitest tests, docs gate green. Committed locally, not yet pushed — pending review. tylermartins.com wiring (route, panels, shared state) is next.
- 2026-08-05 — Built a test suite (pytest for statsbomb, vitest+jsdom for footballd3) and a matching scripts/check_tests.sh gate, wired into the same Stop hook as the docs gate. Caught a real bug along the way: pandas silently turns a None into NaN even in string columns, not just numeric ones — fixed in a new shared utils.clean_nan().
- 2026-08-05 — Built the Player Match Analysis Python extractors (extract_substitutes.py, extract_player_events.py) and footballd3 component extensions/new components (formation.js click-select, convexHull.js points-mode, scrubber.js, cumulativeXtChart.js) on branch player-match-analysis — any non-GK player selectable, not one hardcoded player. tylermartins.com wiring deferred to a future Claude Design → Claude Code handoff.
- 2026-07-29 — Set up shared-context system (scope, style, this progress log) in claude-contexts.
- 2026-07-29 — Shipped match analysis dashboard (desktop + mobile layouts) for a single match.
